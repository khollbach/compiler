{

var x : Integer

function f ( a : Integer, b : Integer) : Boolean
    { if a > = b then
        return true;
      else
        return false
    }

% semantic error should be caught here - x should be an integer, but f returns a boolean
x := f ( 5, 6 )

}