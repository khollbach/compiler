{
var a, b : Integer
var c : Boolean

a := 10
b := 5
c := true

% semantic error - return value from a procedure
procedure p ( a : Integer, b : Integer, c : Boolean )
    { if c then
        return a + b
      else
        return a - b
    }

}