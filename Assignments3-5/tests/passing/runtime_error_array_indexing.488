{
var A[3] : Integer

A[1] := 10
A[2] := 20

% this error will only be caught at runtime, so it passes the syntactic and semantic analysis
if A[3] = 30 then
    write "this should not be possible"
else
    write "runtime error should have been experienced earlier"
}